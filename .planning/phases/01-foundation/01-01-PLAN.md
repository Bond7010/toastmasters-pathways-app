---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pathways-app.html
  - fth-test.html
autonomous: false
requirements:
  - DEPL-01
  - DEPL-03
  - DEPL-04
  - DSGN-01
  - DSGN-02
  - DSGN-03
  - DSGN-04
  - DSGN-05
  - DSGN-06
  - DSGN-07
  - NAV-02
  - PERF-01

must_haves:
  truths:
    - "A throwaway FTH test file with an inline <script> tag has been uploaded to 4039.toastmastersclubs.org and confirmed to execute (script was not stripped)"
    - "pathways-app.html renders the correct 4-zone Grid layout: top bar, path selector, level tab strip, scrollable content area"
    - "The app occupies exactly 100dvh on mobile — no iOS address-bar clipping, no body scroll"
    - "All Toastmasters brand CSS custom properties are declared on :root with exact hex values"
    - "Montserrat (headline) and Source Sans 3 (body) load via Google Fonts with preconnect links"
    - "Required badge renders Loyal Blue background (#004165) with white text"
    - "Elective badge renders Happy Yellow background (#F2DF74) with black text"
    - "Path selector renders as a styled native <select> inside a True Maroon wrapper"
  artifacts:
    - path: "fth-test.html"
      provides: "FTH script-tag compatibility gate"
      contains: "<script>"
    - path: "pathways-app.html"
      provides: "Complete HTML shell and CSS design system"
      contains: "height: 100dvh"
  key_links:
    - from: "meta viewport"
      to: "env(safe-area-inset-bottom)"
      via: "viewport-fit=cover in meta tag"
      pattern: "viewport-fit=cover"
    - from: ":root custom properties"
      to: "badge components"
      via: "CSS var() references"
      pattern: "--tm-loyal-blue.*#004165"
---

<objective>
Gate: Prove FTH accepts inline script tags via a throwaway file, then build the complete HTML shell and CSS design system for pathways-app.html.

Purpose: This plan establishes the two non-negotiable foundations: (1) FTH compatibility is confirmed before any real work is committed, and (2) the brand design system, grid layout, and iOS-safe viewport handling are baked in from the first line of code. Every subsequent plan writes into this shell.

Output:
- fth-test.html — throwaway test file proving FTH does not strip script tags
- pathways-app.html — complete HTML shell with full CSS design system (brand tokens, grid layout, path selector, badge styles, font loading)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\redtr\.claude\toastmasters-pathways-app\.planning\PROJECT.md
@C:\Users\redtr\.claude\toastmasters-pathways-app\.planning\ROADMAP.md
@C:\Users\redtr\.claude\toastmasters-pathways-app\.planning\research\STACK.md
@C:\Users\redtr\.claude\toastmasters-pathways-app\.planning\research\PITFALLS.md

<interfaces>
<!-- CSS design token system to implement verbatim on :root -->
<!-- Source: STACK.md § 8. Design Token System -->

:root {
  /* Tier 1 — Brand values (exact hex — do not approximate) */
  --tm-loyal-blue:   #004165;
  --tm-true-maroon:  #772432;
  --tm-cool-gray:    #A9B2B1;
  --tm-happy-yellow: #F2DF74;
  --tm-white:        #FFFFFF;

  --tm-font-headline: 'Montserrat', 'Gotham', Arial, sans-serif;
  --tm-font-body:     'Source Sans 3', 'Segoe UI', Arial, sans-serif;

  /* Tier 2 — Semantic intent */
  --color-header-bg:           var(--tm-loyal-blue);
  --color-header-text:         var(--tm-white);
  --color-selector-bg:         var(--tm-true-maroon);
  --color-selector-text:       var(--tm-white);
  --color-tab-active:          var(--tm-loyal-blue);
  --color-badge-required-bg:   var(--tm-loyal-blue);
  --color-badge-required-text: var(--tm-white);
  --color-badge-elective-bg:   var(--tm-happy-yellow);
  --color-badge-elective-text: #000000;

  /* Spacing scale (4px base) */
  --space-1: 0.25rem;
  --space-2: 0.5rem;
  --space-3: 0.75rem;
  --space-4: 1rem;
  --space-6: 1.5rem;
  --space-8: 2rem;
}

/* Grid layout (from STACK.md § 2) */
body {
  display: grid;
  grid-template-rows: auto auto auto 1fr;
  /* zones:  top-bar | path-selector | tab-strip | content-area */
  height: 100dvh;   /* NOT 100vh — iOS Safari address-bar bug */
  overflow: hidden; /* body never scrolls */
  margin: 0;
}

/* Content area scrolling (from STACK.md § 3) */
#content-area {
  overflow-y: auto;
  overscroll-behavior: contain;
  /* DO NOT add -webkit-overflow-scrolling: touch */
}

/* FAB positioning (from STACK.md § 6) */
#fab-scroll-top {
  position: fixed;
  bottom: calc(1.5rem + env(safe-area-inset-bottom));
  right: 1.5rem;
}

/* Path selector (from STACK.md § 5) */
.path-selector-wrapper {
  position: relative;
  background: var(--tm-true-maroon);
  padding: 0.5rem 1rem;
}
.path-selector {
  appearance: none;
  -webkit-appearance: none;
  width: 100%;
  background: transparent;
  border: none;
  color: white;
}
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: FTH throwaway script-tag gate test</name>
  <read_first>
    C:\Users\redtr\.claude\toastmasters-pathways-app\.planning\research\PITFALLS.md (C-1 section)
    C:\Users\redtr\.claude\toastmasters-pathways-app\.planning\research\SUMMARY.md (Build Order, Phase 1 step 1)
  </read_first>
  <action>
    Create fth-test.html as a minimal 15-line file with this exact content:

    ```html
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8">
      <title>FTH Script Test</title>
    </head>
    <body>
      <p id="result">JavaScript did not run.</p>
      <script>
        document.getElementById('result').textContent = 'JavaScript ran successfully. Script tags are allowed.';
      </script>
    </body>
    </html>
    ```

    Upload this file to FTH via File Management (NOT the CMS page editor — the editor strips script tags):
    1. Log in to 4039.toastmastersclubs.org admin
    2. Navigate to File Management (not Pages/Content)
    3. Upload fth-test.html as a static file
    4. Access the uploaded file URL directly (not as a CMS page)
    5. Observe the paragraph text

    PASS: Paragraph reads "JavaScript ran successfully. Script tags are allowed."
    FAIL: Paragraph reads "JavaScript did not run." or script tag is missing in view-source.

    If FAIL: Do NOT proceed with pathways-app.html. This phase is blocked. Document the FTH constraint and consult with user about alternatives (e.g., inline onclick handlers, FTH's scripting support, external JS file link).
  </action>
  <verify>
    The uploaded fth-test.html page displays "JavaScript ran successfully. Script tags are allowed." when accessed directly via its static file URL.
  </verify>
  <acceptance_criteria>
    - fth-test.html file exists locally
    - fth-test.html contains the string `<script>`
    - fth-test.html contains `document.getElementById('result').textContent`
    - Human confirms the page displayed the success message when accessed on FTH
  </acceptance_criteria>
  <done>FTH confirms inline script tags survive static file upload. Gate is cleared.</done>
</task>

<task type="auto">
  <name>Task 2: HTML shell + complete CSS design system</name>
  <read_first>
    C:\Users\redtr\.claude\toastmasters-pathways-app\.planning\research\STACK.md (all sections: Layout, Touch Scrolling, Tab Strip, Path Selector, FAB, Fonts, Design Token System)
    C:\Users\redtr\.claude\toastmasters-pathways-app\.planning\research\PITFALLS.md (C-3, M-1, M-2, M-3, N-2)
    C:\Users\redtr\.claude\toastmasters-pathways-app\.planning\PROJECT.md
    C:\Users\redtr\.claude\toastmasters-pathways-app\CLAUDE.md
  </read_first>
  <action>
    Create pathways-app.html as a complete single-file HTML document. The file must contain ALL of the following in exact order:

    **1. DOCTYPE and head:**
    ```html
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
      <title>Toastmasters Pathways</title>
      <link rel="preconnect" href="https://fonts.googleapis.com">
      <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
      <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&family=Source+Sans+3:wght@400;600&display=swap" rel="stylesheet">
      <style>
        /* ... all CSS here ... */
      </style>
    </head>
    ```

    **2. CSS on :root — exact token values (no approximation):**
    All Tier 1 brand tokens: `--tm-loyal-blue: #004165`, `--tm-true-maroon: #772432`, `--tm-cool-gray: #A9B2B1`, `--tm-happy-yellow: #F2DF74`, `--tm-white: #FFFFFF`, plus the two font variables and all Tier 2 semantic tokens and spacing scale as shown in the interfaces block above.

    **3. CSS body grid layout:**
    ```css
    *, *::before, *::after { box-sizing: border-box; }
    body {
      display: grid;
      grid-template-rows: auto auto auto 1fr;
      height: 100dvh;
      overflow: hidden;
      margin: 0;
      font-family: var(--tm-font-body);
      font-size: 16px;
    }
    ```

    **4. CSS for each zone:**

    Top bar (`#top-bar`):
    ```css
    #top-bar {
      background: var(--color-header-bg);
      color: var(--color-header-text);
      padding: var(--space-3) var(--space-4);
      font-family: var(--tm-font-headline);
      font-size: 1.25rem;
      font-weight: 700;
    }
    ```

    Path selector zone (`#path-selector-zone`): Wrap a native `<select>` inside a `<div class="path-selector-wrapper">`. The wrapper has `background: var(--color-selector-bg)` and a CSS `::after` chevron (`content: "▾"; position: absolute; right: 1rem; color: white; pointer-events: none`). The `<select>` itself: `appearance: none; -webkit-appearance: none; width: 100%; background: transparent; border: none; color: white; font-family: var(--tm-font-headline); font-size: 1rem; padding: var(--space-2) 0; cursor: pointer; outline-offset: 2px`.

    Level tab strip (`#tab-strip`): `display: flex; overflow-x: auto; overscroll-behavior-x: contain; scroll-snap-type: x proximity; scrollbar-width: none; background: #f5f5f5; border-bottom: 1px solid #ddd`. Add `::-webkit-scrollbar { display: none }` rule. Each `[role="tab"]`: `flex-shrink: 0; scroll-snap-align: start; white-space: nowrap; padding: var(--space-3) var(--space-4); min-height: 44px; border: none; border-bottom: 3px solid transparent; background: transparent; cursor: pointer; font-family: var(--tm-font-headline); font-size: 0.875rem; color: #333`. Active state: `[role="tab"][aria-selected="true"] { border-bottom-color: var(--color-tab-active); color: var(--tm-loyal-blue); font-weight: 700 }`.

    Content area (`#content-area`):
    ```css
    #content-area {
      overflow-y: auto;
      overscroll-behavior: contain;
      padding: var(--space-4);
      background: #f9f9f9;
    }
    /* DO NOT add -webkit-overflow-scrolling */
    ```

    Content pane (`#content-pane`): No special CSS — it is a plain `<div>` that render() targets with innerHTML.

    **5. Badge styles:**
    ```css
    .badge {
      display: inline-block;
      padding: var(--space-1) var(--space-2);
      border-radius: 3px;
      font-size: 0.75rem;
      font-weight: 700;
      letter-spacing: 0.05em;
      text-transform: uppercase;
    }
    .badge-required {
      background: var(--color-badge-required-bg);
      color: var(--color-badge-required-text);
    }
    .badge-elective {
      background: var(--color-badge-elective-bg);
      color: var(--color-badge-elective-text);
    }
    /* NEVER use --tm-happy-yellow as text color on light bg — fails WCAG */
    ```

    **6. Project card styles:**
    ```css
    .project-card {
      background: white;
      border: 1px solid #e0e0e0;
      border-radius: 6px;
      padding: var(--space-4);
      margin-bottom: var(--space-3);
    }
    .project-card-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: var(--space-2);
      margin-bottom: var(--space-2);
    }
    .project-name {
      font-family: var(--tm-font-headline);
      font-weight: 700;
      font-size: 1rem;
      margin: 0;
    }
    .project-time {
      font-size: 0.875rem;
      color: #555;
      margin: var(--space-1) 0;
    }
    .project-purpose {
      font-size: 0.9375rem;
      color: #333;
      line-height: 1.5;
      margin: 0;
    }
    ```

    **7. Enhancement section styles:**
    ```css
    .enhancement-section {
      background: white;
      border: 2px solid var(--tm-loyal-blue);
      border-radius: 6px;
      padding: var(--space-4);
      margin-bottom: var(--space-4);
    }
    .enhancement-title {
      font-family: var(--tm-font-headline);
      font-weight: 700;
      color: var(--tm-loyal-blue);
      margin: 0 0 var(--space-2) 0;
      font-size: 0.875rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }
    .enhancement-roles {
      list-style: none;
      padding: 0;
      margin: 0;
      display: flex;
      flex-wrap: wrap;
      gap: var(--space-2);
    }
    .enhancement-roles li {
      background: #e8eef2;
      color: var(--tm-loyal-blue);
      padding: var(--space-1) var(--space-3);
      border-radius: 3px;
      font-size: 0.875rem;
    }
    .enhancement-notes {
      margin: var(--space-2) 0 0 0;
      font-size: 0.875rem;
      color: #555;
      font-style: italic;
    }
    ```

    **8. Level header styles:**
    ```css
    .level-header {
      margin-bottom: var(--space-4);
    }
    .level-name {
      font-family: var(--tm-font-headline);
      font-weight: 700;
      font-size: 1.25rem;
      color: var(--tm-loyal-blue);
      margin: 0 0 var(--space-1) 0;
    }
    .level-completion-req {
      font-size: 0.9375rem;
      color: #555;
      margin: 0;
    }
    ```

    **9. FAB styles:**
    ```css
    #fab-scroll-top {
      position: fixed;
      bottom: calc(1.5rem + env(safe-area-inset-bottom));
      right: 1.5rem;
      width: 3rem;
      height: 3rem;
      border-radius: 50%;
      background: var(--tm-loyal-blue);
      color: white;
      border: none;
      cursor: pointer;
      box-shadow: 0 4px 8px rgba(0,0,0,0.3);
      z-index: 100;
      font-size: 1.25rem;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    #fab-scroll-top[hidden] { display: none; }
    ```

    **10. Responsive breakpoint (>=768px):**
    ```css
    @media (min-width: 768px) {
      body {
        max-width: 900px;
        margin: 0 auto;
      }
      .project-card {
        padding: var(--space-6);
      }
    }
    ```

    **11. Focus indicator (accessibility — never remove):**
    ```css
    :focus-visible {
      outline: 2px solid var(--tm-loyal-blue);
      outline-offset: 2px;
    }
    ```

    **12. HTML body structure (stub — will be filled by Plans 02 and 03):**
    ```html
    <body>
      <header id="top-bar" role="banner">Pathways</header>

      <div id="path-selector-zone">
        <div class="path-selector-wrapper">
          <select id="path-selector" class="path-selector" aria-label="Select a Pathways path">
            <option value="dynamic-leadership">Dynamic Leadership</option>
            <option value="communication-series">Communication Series</option>
          </select>
        </div>
      </div>

      <div id="tab-strip" role="tablist" aria-label="Level tabs">
        <!-- Tabs rendered by render() — placeholder tabs here for layout smoke test -->
        <button role="tab" aria-selected="true" tabindex="0"
                aria-controls="content-pane" id="tab-l0">Level 1</button>
        <button role="tab" aria-selected="false" tabindex="-1"
                aria-controls="content-pane" id="tab-l1">Level 2</button>
        <button role="tab" aria-selected="false" tabindex="-1"
                aria-controls="content-pane" id="tab-l2">Level 3</button>
        <button role="tab" aria-selected="false" tabindex="-1"
                aria-controls="content-pane" id="tab-l3">Level 4</button>
        <button role="tab" aria-selected="false" tabindex="-1"
                aria-controls="content-pane" id="tab-l4">Level 5</button>
      </div>

      <div id="content-area">
        <div id="scroll-sentinel"></div>
        <div id="content-pane" role="tabpanel" tabindex="0"
             aria-labelledby="tab-l0">
          <!-- Content rendered by render() in Plan 02 -->
          <p style="color:#999;padding:1rem">Content will appear here after Plan 02 and 03 are complete.</p>
        </div>
      </div>

      <button id="fab-scroll-top" hidden aria-label="Back to top">&#8679;</button>

      <!-- Data block: Plan 02 will replace this stub -->
      <script type="application/json" id="pathways-data">
      []
      </script>

      <script>
        /* Plan 02: render pipeline | Plan 03: events + keyboard + FAB */
      </script>
    </body>
    </html>
    ```

    Confirm the file is self-contained: all CSS is inside `<style>` in `<head>`, no external CSS links beyond Google Fonts, no external JS.
  </action>
  <verify>
    <automated>powershell.exe -Command "Select-String -Path 'C:\Users\redtr\.claude\toastmasters-pathways-app\pathways-app.html' -Pattern 'height: 100dvh' | Select-Object -First 1"</automated>
  </verify>
  <acceptance_criteria>
    - pathways-app.html contains `height: 100dvh`
    - pathways-app.html does NOT contain `100vh` (check with grep excluding comments)
    - pathways-app.html does NOT contain `-webkit-overflow-scrolling`
    - pathways-app.html contains `--tm-loyal-blue: #004165`
    - pathways-app.html contains `--tm-true-maroon: #772432`
    - pathways-app.html contains `--tm-cool-gray: #A9B2B1`
    - pathways-app.html contains `--tm-happy-yellow: #F2DF74`
    - pathways-app.html contains `viewport-fit=cover`
    - pathways-app.html contains `env(safe-area-inset-bottom)`
    - pathways-app.html contains `scroll-snap-type: x proximity`
    - pathways-app.html contains `flex-shrink: 0`
    - pathways-app.html contains `white-space: nowrap`
    - pathways-app.html contains `overscroll-behavior: contain`
    - pathways-app.html contains `font-family: Montserrat`
    - pathways-app.html contains `family=Source+Sans+3`
    - pathways-app.html contains `rel="preconnect" href="https://fonts.googleapis.com"`
    - pathways-app.html contains `rel="preconnect" href="https://fonts.gstatic.com" crossorigin`
    - pathways-app.html contains `badge-required` and `badge-elective` CSS classes
    - pathways-app.html contains `--color-badge-elective-text: #000000`
    - pathways-app.html contains `role="tablist"`
    - pathways-app.html contains `role="tabpanel"`
    - pathways-app.html contains `aria-label="Back to top"`
    - pathways-app.html contains `id="fab-scroll-top"`
    - pathways-app.html contains `script type="application/json" id="pathways-data"`
    - pathways-app.html contains `grid-template-rows: auto auto auto 1fr`
    - pathways-app.html contains `appearance: none`
    - pathways-app.html contains `:focus-visible`
  </acceptance_criteria>
  <done>pathways-app.html opens in a browser and shows the 4-zone layout (blue top bar, maroon path selector, tab strip, gray content area) at all viewport widths including 320px. No overflow on body. Fonts render as Montserrat and Source Sans 3.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Visual layout verification at 320px and on iOS Safari</name>
  <read_first>
    pathways-app.html (just written by Task 2)
  </read_first>
  <what-built>
    pathways-app.html with the complete 4-zone grid layout and CSS design system. Brand colors, fonts, grid, path selector, tab strip, FAB, and badge styles are in place.
  </what-built>
  <how-to-verify>
    1. Open pathways-app.html in Chrome Desktop
    2. Open Chrome DevTools → set viewport to 320px wide
    3. Verify: Blue top bar visible. Maroon path selector below it. Tab strip with 5 tabs, scrollable horizontally (drag to confirm scroll works). Gray content area below tabs.
    4. Verify: Body does NOT scroll (only the content area inside the grid scrolls).
    5. Verify: All 5 tabs fit or scroll within the 320px width — no tab text wraps to a second line.
    6. If possible, open on real iOS Safari: confirm address bar does not clip the bottom of the content area, and touch-scrolling in the content area works with momentum.
    7. Inspect: Confirm "Pathways" top bar is #004165 blue, path selector is #772432 maroon.
    8. Inspect: Confirm font is Montserrat (or Arial fallback if offline).
  </how-to-verify>
  <resume-signal>Type "approved" if the layout is correct, or describe any visual issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FTH file upload → browser | Static HTML served by FTH; no server-side processing |
| Google Fonts CDN → browser | External CSS/font fetch at page load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Google Fonts @import | accept | Audience is always online; Google Fonts CDN is high-availability. Font failure degrades to Arial fallback — layout intact. |
| T-01-02 | Information Disclosure | HTML source file | accept | No PII or secrets in the file. All data is public Toastmasters curriculum. |
| T-01-03 | Denial of Service | FTH static file hosting | accept | FTH downtime affects the whole club site, not specifically this file. No mitigation possible within scope. |
</threat_model>

<verification>
After Task 2 completes, run these grep checks against pathways-app.html:

```
grep "height: 100dvh" pathways-app.html       → must match
grep "100vh" pathways-app.html                → must NOT match (excluding dvh)
grep "\-webkit\-overflow\-scrolling" pathways-app.html → must NOT match
grep "#004165" pathways-app.html              → must match
grep "#772432" pathways-app.html              → must match
grep "#F2DF74" pathways-app.html              → must match
grep "viewport-fit=cover" pathways-app.html   → must match
grep "safe-area-inset-bottom" pathways-app.html → must match
```
</verification>

<success_criteria>
- fth-test.html uploaded to FTH and script execution confirmed by human (DEPL-04, DEPL-03)
- pathways-app.html is a single self-contained HTML file (DEPL-01)
- 4-zone grid layout renders correctly at 320px and desktop widths (DSGN-05, DSGN-06)
- body uses 100dvh; no iOS address-bar clipping (DSGN-07)
- All brand CSS custom properties present with exact hex values (DSGN-01)
- Montserrat and Source Sans 3 loaded via Google Fonts with preconnect (DSGN-02)
- Required badge CSS: Loyal Blue bg, white text (DSGN-03)
- Elective badge CSS: Happy Yellow bg, black text (DSGN-04)
- Native `<select>` present in maroon wrapper (NAV-02)
- Only content-area scrolls; content-pane innerHTML is render() target (PERF-01 architectural gate)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` following the summary template.
</output>
