---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - pathways-app.html
autonomous: true
requirements:
  - DATA-04
  - DATA-05
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06
  - CONT-07
  - NAV-01
  - NAV-03

must_haves:
  truths:
    - "pathways-app.html contains a <script type='application/json' id='pathways-data'> block with 2 path objects"
    - "Dynamic Leadership path data includes a non-null enhancement object with roles[] and notes fields"
    - "Communication Series path data has enhancement: null (vintage path — no Enhancement section)"
    - "The PATHWAYS constant is parsed from the JSON block on load (not a JS object literal)"
    - "Selecting Dynamic Leadership Level 1 renders an Enhancement section above the project cards"
    - "Selecting Communication Series Level 1 renders NO Enhancement section"
    - "Each project card shows name, Required/Elective badge, time, and purpose inline"
    - "The render pipeline calls only buildEnhancementHTML() and buildProjectCardHTML() — no direct DOM manipulation outside render()"
  artifacts:
    - path: "pathways-app.html"
      provides: "Embedded JSON data block + render pipeline functions"
      contains: "script type=\"application/json\" id=\"pathways-data\""
    - path: "pathways-app.html"
      provides: "buildProjectCardHTML function"
      contains: "buildProjectCardHTML"
    - path: "pathways-app.html"
      provides: "buildEnhancementHTML function"
      contains: "buildEnhancementHTML"
    - path: "pathways-app.html"
      provides: "render() function"
      contains: "function render()"
  key_links:
    - from: "#pathways-data script block"
      to: "PATHWAYS constant"
      via: "JSON.parse(document.getElementById('pathways-data').text)"
      pattern: "JSON.parse.*pathways-data"
    - from: "render()"
      to: "#content-pane"
      via: "contentPane.innerHTML = buildLevelHTML(...)"
      pattern: "contentPane.innerHTML"
    - from: "level.enhancement"
      to: "buildEnhancementHTML()"
      via: "null check before calling renderer"
      pattern: "enhancement.*null"
---

<objective>
Embed the placeholder JSON data for 2 paths and implement the full render pipeline: `buildProjectCardHTML()`, `buildEnhancementHTML()`, `buildLevelHTML()`, and `render()`.

Purpose: This plan wires the data layer to the DOM. After this plan, navigating to any path+level in the placeholder data (Dynamic Leadership and Communication Series) renders the correct content without any user gesture beyond page load. The render pipeline established here is the final architecture — Plans 02 and 03 in Phase 2 will only add more data entries without changing the pipeline.

Output: pathways-app.html updated with embedded JSON data and render pipeline functions. Default path (Dynamic Leadership Level 1) renders automatically on page load.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\redtr\.claude\toastmasters-pathways-app\.planning\PROJECT.md
@C:\Users\redtr\.claude\toastmasters-pathways-app\.planning\ROADMAP.md
@C:\Users\redtr\.claude\toastmasters-pathways-app\.planning\research\ARCHITECTURE.md
@C:\Users\redtr\.claude\toastmasters-pathways-app\.planning\research\PITFALLS.md
@C:\Users\redtr\.claude\toastmasters-pathways-app\CLAUDE.md

<interfaces>
<!-- JSON schema (from ARCHITECTURE.md) — implement verbatim, no deviations -->

Top-level array structure:
[
  {
    "id": "dynamic-leadership",       // slug: lowercase, hyphenated
    "name": "Dynamic Leadership",
    "levels": [
      {
        "level": 1,                   // 1-based integer
        "name": "Mastering Fundamentals",
        "completionRequirement": "Complete 4 required projects and all enhancement tasks",
        "enhancement": {              // non-null for current paths
          "roles": [
            "Table Topic Speaker",
            "Evaluator",
            "Timer OR Ah-Counter"
          ],
          "notes": "Table Topics Speaker must be completed before delivering an Ice Breaker speech"
        },
        "projects": [
          {
            "name": "Ice Breaker",
            "type": "Required",       // strict enum: "Required" | "Elective"
            "time": "4–6 min",        // en-dash, abbreviated unit, no "minute" word
            "purpose": "..."
          }
        ]
      }
    ]
  }
]

// Vintage path schema difference:
// "enhancement": null    ← NOT an object; null means no Enhancement section rendered

// dataVersion and lastVerified at root level (wrap the array in an object):
{
  "dataVersion": "2026-05",
  "lastVerified": "2026-05-27",
  "paths": [ ... ]
}

// Parse accordingly:
// const DATA = JSON.parse(document.getElementById('pathways-data').text);
// const PATHWAYS = DATA.paths;
// Store DATA.dataVersion and DATA.lastVerified for footer display.

<!-- State singleton (from ARCHITECTURE.md) -->
const state = {
  selectedPath: 'dynamic-leadership',  // default on load
  selectedLevel: 0                      // 0-based index into levels[]
};

function setState(updates) {
  Object.assign(state, updates);
  render();
}

<!-- Render pipeline function signatures (from ARCHITECTURE.md) -->
function buildProjectCardHTML(project, pathId, levelIndex)
  // Returns HTML string for one project card
  // pathId and levelIndex used for unique DOM IDs: id="card-{pathId}-l{levelIndex}-p{projectIndex}"
  // Badge class: project.type === 'Required' ? 'badge-required' : 'badge-elective'
  // NEVER renders directly to DOM — returns string only

function buildEnhancementHTML(enhancement, pathId, levelIndex)
  // Returns HTML string for the enhancement section
  // Called ONLY when enhancement !== null
  // Returns '' when enhancement is null (caller must check)

function buildLevelHTML(level, pathId, levelIndex)
  // Calls buildEnhancementHTML and buildProjectCardHTML for each project
  // Returns complete HTML string for the content pane

function render()
  // Reads state.selectedPath and state.selectedLevel
  // Looks up: const pathData = PATHWAYS.find(p => p.id === state.selectedPath)
  // Calls buildLevelHTML with the appropriate level object
  // Writes result: document.getElementById('content-pane').innerHTML = html
  // Updates tab active states (5 classList/setAttribute calls)
  // DOES NOT fire events, DOES NOT read from contentPane

<!-- DOM ID convention (from CLAUDE.md — critical to avoid collisions) -->
// Tab IDs:   id="tab-{pathId}-l{levelIndex}"    e.g. "tab-dynamic-leadership-l0"
// Panel ID:  id="panel-{pathId}-l{levelIndex}"  e.g. "panel-dynamic-leadership-l0"
// Card IDs:  id="card-{pathId}-l{levelIndex}-p{projectIndex}"
// NEVER use just "tab-l0" or "panel-l0" — Ice Breaker appears in all 8 paths
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Embed placeholder JSON data for 2 paths</name>
  <read_first>
    C:\Users\redtr\.claude\toastmasters-pathways-app\pathways-app.html (read current state before modifying)
    C:\Users\redtr\.claude\toastmasters-pathways-app\.planning\research\ARCHITECTURE.md (Data Schema section)
    C:\Users\redtr\.claude\toastmasters-pathways-app\.planning\research\PITFALLS.md (C-4, C-5, N-3, N-5)
    C:\Users\redtr\.claude\toastmasters-pathways-app\CLAUDE.md (Conventions section)
  </read_first>
  <action>
    Replace the stub `<script type="application/json" id="pathways-data">[]</script>` in pathways-app.html with the full placeholder JSON for 2 paths. The JSON must be wrapped in a root object (not a bare array) to include metadata fields.

    The JSON content to embed (exact structure — do not deviate from schema):

    ```json
    {
      "dataVersion": "2026-05",
      "lastVerified": "2026-05-27",
      "paths": [
        {
          "id": "dynamic-leadership",
          "name": "Dynamic Leadership",
          "levels": [
            {
              "level": 1,
              "name": "Mastering Fundamentals",
              "completionRequirement": "Complete 4 required projects and all enhancement tasks",
              "enhancement": {
                "roles": [
                  "Table Topic Speaker",
                  "Evaluator",
                  "Timer OR Ah-Counter"
                ],
                "notes": "Table Topics Speaker must be completed before delivering an Ice Breaker speech"
              },
              "projects": [
                {
                  "name": "Ice Breaker",
                  "type": "Required",
                  "time": "4–6 min",
                  "purpose": "Deliver a speech about yourself to introduce yourself to the club and learn the basic structure of public speaking."
                },
                {
                  "name": "Writing a Speech with Purpose",
                  "type": "Required",
                  "time": "5–7 min",
                  "purpose": "Write and deliver a speech on a topic of your choice, demonstrating that you can organize ideas logically."
                },
                {
                  "name": "Introduction to Vocal Variety and Body Language",
                  "type": "Required",
                  "time": "5–7 min",
                  "purpose": "Learn to use your voice and body to enhance your message and engage your audience."
                },
                {
                  "name": "Evaluation and Feedback",
                  "type": "Required",
                  "time": "5–7 min",
                  "purpose": "Learn how to give and receive constructive feedback by delivering a speech and evaluating a fellow member."
                },
                {
                  "name": "Researching and Presenting",
                  "type": "Elective",
                  "time": "5–7 min",
                  "purpose": "Research a topic thoroughly and present your findings in a clear, organized speech."
                }
              ]
            },
            {
              "level": 2,
              "name": "Learning Your Style",
              "completionRequirement": "Complete 2 required projects, 1 elective project, and all enhancement tasks",
              "enhancement": {
                "roles": [
                  "Toastmaster of the Evening",
                  "General Evaluator"
                ],
                "notes": ""
              },
              "projects": [
                {
                  "name": "Understanding Your Communication Style",
                  "type": "Required",
                  "time": "5–7 min",
                  "purpose": "Identify your personal communication style and deliver a speech that plays to your strengths."
                },
                {
                  "name": "Effective Body Language",
                  "type": "Required",
                  "time": "5–7 min",
                  "purpose": "Demonstrate deliberate use of body language to reinforce your verbal message."
                },
                {
                  "name": "Connect with Storytelling",
                  "type": "Elective",
                  "time": "5–7 min",
                  "purpose": "Use storytelling techniques to connect with your audience on an emotional level."
                },
                {
                  "name": "Creating Effective Visual Aids",
                  "type": "Elective",
                  "time": "5–7 min",
                  "purpose": "Design and use visual aids that enhance your speech without overwhelming your audience."
                }
              ]
            },
            {
              "level": 3,
              "name": "Increasing Knowledge",
              "completionRequirement": "Complete 1 required project, 2 elective projects, and all enhancement tasks",
              "enhancement": {
                "roles": [
                  "Prepared Speaker Coach",
                  "Mentorship activities"
                ],
                "notes": ""
              },
              "projects": [
                {
                  "name": "Inspire Your Audience",
                  "type": "Required",
                  "time": "7–10 min",
                  "purpose": "Craft and deliver a speech designed to motivate your audience to take action or change a belief."
                },
                {
                  "name": "Persuasive Influence",
                  "type": "Elective",
                  "time": "5–7 min",
                  "purpose": "Persuade your audience to adopt your viewpoint using evidence, logic, and emotional appeal."
                },
                {
                  "name": "The Power of Humor",
                  "type": "Elective",
                  "time": "5–7 min",
                  "purpose": "Incorporate appropriate humor to engage your audience and lighten complex topics."
                }
              ]
            },
            {
              "level": 4,
              "name": "Building Skills",
              "completionRequirement": "Complete 1 required project, 1 elective project, and all enhancement tasks",
              "enhancement": {
                "roles": [
                  "Club Officer or Committee Chair activities"
                ],
                "notes": ""
              },
              "projects": [
                {
                  "name": "Leading in Your Volunteer Organization",
                  "type": "Required",
                  "time": "5–7 min",
                  "purpose": "Describe a leadership experience in a volunteer organization and the lessons you learned."
                },
                {
                  "name": "Managing a Difficult Audience",
                  "type": "Elective",
                  "time": "12–15 min",
                  "purpose": "Deliver a speech to a deliberately challenging audience and practice managing disruptions professionally."
                }
              ]
            },
            {
              "level": 5,
              "name": "Demonstrating Expertise",
              "completionRequirement": "Complete 2 required projects and all enhancement tasks",
              "enhancement": {
                "roles": [
                  "Serve in a club leadership role for at least 6 months"
                ],
                "notes": ""
              },
              "projects": [
                {
                  "name": "High Performance Leadership",
                  "type": "Required",
                  "time": "Project-based",
                  "purpose": "Plan and execute a significant project for your club or community, then present a report to your club."
                },
                {
                  "name": "Prepare to Speak Professionally",
                  "type": "Required",
                  "time": "18–22 min",
                  "purpose": "Develop and deliver a professional-level presentation suitable for an outside audience."
                }
              ]
            }
          ]
        },
        {
          "id": "communication-series",
          "name": "Communication Series",
          "levels": [
            {
              "level": 1,
              "name": "Introduction to Basic Training",
              "completionRequirement": "Complete all required projects",
              "enhancement": null,
              "projects": [
                {
                  "name": "Ice Breaker",
                  "type": "Required",
                  "time": "4–6 min",
                  "purpose": "Introduce yourself to your club with a brief prepared speech."
                },
                {
                  "name": "Speech 2",
                  "type": "Required",
                  "time": "5–7 min",
                  "purpose": "Deliver a well-organized speech on a topic of your choice using clear transitions and a logical structure."
                }
              ]
            },
            {
              "level": 2,
              "name": "Level 2",
              "completionRequirement": "Complete all required projects",
              "enhancement": null,
              "projects": [
                {
                  "name": "Speech 3",
                  "type": "Required",
                  "time": "5–7 min",
                  "purpose": "Practice getting to the point quickly and organizing your ideas around a single theme."
                },
                {
                  "name": "Speech 4",
                  "type": "Required",
                  "time": "5–7 min",
                  "purpose": "Develop your use of the body in public speaking through gesture, movement, and eye contact."
                }
              ]
            },
            {
              "level": 3,
              "name": "Level 3",
              "completionRequirement": "Complete all required projects",
              "enhancement": null,
              "projects": [
                {
                  "name": "Speech 5",
                  "type": "Required",
                  "time": "5–7 min",
                  "purpose": "Use vocal variety — including pitch, rate, volume, and pauses — to make your speech more expressive."
                },
                {
                  "name": "Speech 6",
                  "type": "Required",
                  "time": "5–7 min",
                  "purpose": "Apply what you have learned about body language and vocal variety in a fully integrated speech."
                }
              ]
            },
            {
              "level": 4,
              "name": "Level 4",
              "completionRequirement": "Complete all required projects",
              "enhancement": null,
              "projects": [
                {
                  "name": "Speech 7",
                  "type": "Required",
                  "time": "5–7 min",
                  "purpose": "Research a topic and present your findings in a fact-based, well-organized speech."
                },
                {
                  "name": "Speech 8",
                  "type": "Required",
                  "time": "5–7 min",
                  "purpose": "Persuade your audience to agree with your position using logic and evidence."
                },
                {
                  "name": "Speech 9",
                  "type": "Required",
                  "time": "5–7 min",
                  "purpose": "Inspire your audience with a speech built around an emotional appeal and a strong call to action."
                }
              ]
            },
            {
              "level": 5,
              "name": "Level 5",
              "completionRequirement": "Complete all required projects",
              "enhancement": null,
              "projects": [
                {
                  "name": "Speech 10",
                  "type": "Required",
                  "time": "8–10 min",
                  "purpose": "Deliver an inspiring speech that demonstrates your full growth as a communicator."
                },
                {
                  "name": "Speech 11",
                  "type": "Required",
                  "time": "8–10 min",
                  "purpose": "Further demonstrate your communication mastery with a speech that showcases your personal style."
                },
                {
                  "name": "Speech 12",
                  "type": "Required",
                  "time": "8–10 min",
                  "purpose": "Deliver your capstone speech — a polished presentation that represents the best of your communication skills."
                }
              ]
            }
          ]
        }
      ]
    }
    ```

    IMPORTANT: The `–` Unicode escape represents the en-dash (–) in JSON. Use `–` in the JSON source so the en-dash is preserved without editor issues. Verify in browser that time fields render as "4–6 min" (not "4-6 min" or "4â€"6 min").

    Also update the path `<select>` options to reflect both paths:
    ```html
    <option value="dynamic-leadership">Dynamic Leadership</option>
    <option value="communication-series">Communication Series</option>
    ```
  </action>
  <verify>
    <automated>powershell.exe -Command "Select-String -Path 'C:\Users\redtr\.claude\toastmasters-pathways-app\pathways-app.html' -Pattern 'dataVersion' | Select-Object -First 1"</automated>
  </verify>
  <acceptance_criteria>
    - pathways-app.html contains `"dataVersion": "2026-05"`
    - pathways-app.html contains `"lastVerified": "2026-05-27"`
    - pathways-app.html contains `"id": "dynamic-leadership"`
    - pathways-app.html contains `"id": "communication-series"`
    - pathways-app.html contains `"enhancement": null` (for Communication Series levels)
    - pathways-app.html contains `"type": "Required"` (capital R — strict enum)
    - pathways-app.html contains `"type": "Elective"` (capital E — strict enum)
    - pathways-app.html does NOT contain `"type": "required"` (lowercase — wrong)
    - pathways-app.html does NOT contain `"type": "elective"` (lowercase — wrong)
    - pathways-app.html contains `–` OR `–` in time fields (en-dash present)
    - pathways-app.html does NOT contain bare `-` in time values like `"4-6 min"` (hyphen is wrong)
    - pathways-app.html contains `"paths":` (JSON wrapped in root object with metadata)
  </acceptance_criteria>
  <done>JSON data block contains 2 paths (10 levels total). Dynamic Leadership has non-null enhancement on all 5 levels. Communication Series has null enhancement on all 5 levels. Time values use en-dashes.</done>
</task>

<task type="auto">
  <name>Task 2: Render pipeline — buildProjectCardHTML, buildEnhancementHTML, buildLevelHTML, render()</name>
  <read_first>
    C:\Users\redtr\.claude\toastmasters-pathways-app\pathways-app.html (current state after Task 1)
    C:\Users\redtr\.claude\toastmasters-pathways-app\.planning\research\ARCHITECTURE.md (Prescriptive Decisions, Anti-Patterns)
    C:\Users\redtr\.claude\toastmasters-pathways-app\CLAUDE.md (Architecture and Conventions sections)
  </read_first>
  <action>
    Replace the stub `/* Plan 02: render pipeline | Plan 03: events + keyboard + FAB */` comment inside the main `<script>` block with the complete render pipeline. Do NOT create a new `<script>` tag — write all JS into the existing `<script>` block.

    Implement in this exact order within the script block:

    **1. Parse data from the JSON block:**
    ```javascript
    const DATA = JSON.parse(document.getElementById('pathways-data').text);
    const PATHWAYS = DATA.paths;
    // PATHWAYS is the array of path objects. Access by find(), not by index.
    ```

    **2. State singleton:**
    ```javascript
    const state = {
      selectedPath: 'dynamic-leadership',
      selectedLevel: 0  // 0-based index into levels[]
    };

    function setState(updates) {
      Object.assign(state, updates);
      render();
    }
    ```

    **3. buildProjectCardHTML(project, pathId, levelIndex, projectIndex):**
    Returns an HTML string. Uses CSS classes from Plan 01's design system.
    ```javascript
    function buildProjectCardHTML(project, pathId, levelIndex, projectIndex) {
      const badgeClass = project.type === 'Required' ? 'badge-required' : 'badge-elective';
      const cardId = `card-${pathId}-l${levelIndex}-p${projectIndex}`;
      return `
        <div class="project-card" id="${cardId}">
          <div class="project-card-header">
            <h3 class="project-name">${project.name}</h3>
            <span class="badge ${badgeClass}">${project.type}</span>
          </div>
          <p class="project-time">${project.time}</p>
          <p class="project-purpose">${project.purpose}</p>
        </div>
      `;
    }
    ```

    **4. buildEnhancementHTML(enhancement, pathId, levelIndex):**
    Returns '' when enhancement is null. Returns a labeled section with roles as pill items.
    ```javascript
    function buildEnhancementHTML(enhancement, pathId, levelIndex) {
      if (!enhancement) return '';
      const rolesHTML = enhancement.roles
        .map(role => `<li>${role}</li>`)
        .join('');
      const notesHTML = enhancement.notes
        ? `<p class="enhancement-notes">${enhancement.notes}</p>`
        : '';
      return `
        <section class="enhancement-section" aria-labelledby="enhancement-title-${pathId}-l${levelIndex}">
          <h3 class="enhancement-title" id="enhancement-title-${pathId}-l${levelIndex}">Enhancement</h3>
          <p class="enhancement-subtitle">Complete these meeting roles during this level:</p>
          <ul class="enhancement-roles">${rolesHTML}</ul>
          ${notesHTML}
        </section>
      `;
    }
    ```

    Add to CSS (inside the `<style>` block): `.enhancement-subtitle { font-size: 0.875rem; color: #555; margin: 0 0 var(--space-2) 0; }`

    **5. buildLevelHTML(level, pathId, levelIndex):**
    Orchestrates buildEnhancementHTML and buildProjectCardHTML.
    ```javascript
    function buildLevelHTML(level, pathId, levelIndex) {
      const enhancementHTML = buildEnhancementHTML(level.enhancement, pathId, levelIndex);
      const cardsHTML = level.projects
        .map((project, i) => buildProjectCardHTML(project, pathId, levelIndex, i))
        .join('');
      return `
        <div class="level-header">
          <h2 class="level-name">Level ${level.level}: ${level.name}</h2>
          <p class="level-completion-req">${level.completionRequirement}</p>
        </div>
        ${enhancementHTML}
        ${cardsHTML}
      `;
    }
    ```

    **6. render() — reads state, writes to DOM once:**
    ```javascript
    function render() {
      const pathData = PATHWAYS.find(p => p.id === state.selectedPath);
      if (!pathData) return;

      const level = pathData.levels[state.selectedLevel];
      if (!level) return;

      // Update content pane (single innerHTML assignment — render-on-demand)
      const contentPane = document.getElementById('content-pane');
      contentPane.innerHTML = buildLevelHTML(level, state.selectedPath, state.selectedLevel);

      // Update panel ARIA labelledby to match active tab
      const tabId = `tab-${state.selectedPath}-l${state.selectedLevel}`;
      contentPane.setAttribute('aria-labelledby', tabId);

      // Update tab active states (Plan 03 will set up the tab elements with correct IDs;
      // render() uses querySelectorAll and sets aria-selected + tabindex)
      document.querySelectorAll('[role="tab"]').forEach((tab, i) => {
        const isActive = i === state.selectedLevel;
        tab.setAttribute('aria-selected', isActive ? 'true' : 'false');
        tab.setAttribute('tabindex', isActive ? '0' : '-1');
      });
    }
    ```

    **7. Rebuild tabs from path data (called by render or on path change):**
    Since Plan 01 put static placeholder tabs in HTML, the render pipeline must rebuild the tab strip dynamically to use correct IDs and level names. Add this function:
    ```javascript
    function buildTabsHTML(pathData) {
      return pathData.levels.map((level, i) => {
        const tabId = `tab-${pathData.id}-l${i}`;
        const panelId = 'content-pane';
        const isActive = i === state.selectedLevel;
        return `<button role="tab"
                  id="${tabId}"
                  aria-selected="${isActive ? 'true' : 'false'}"
                  tabindex="${isActive ? '0' : '-1'}"
                  aria-controls="${panelId}"
                  data-level-index="${i}">Level ${level.level}</button>`;
      }).join('');
    }
    ```

    Update render() to also rebuild the tab strip when the path changes. Add at the start of render():
    ```javascript
    function render() {
      const pathData = PATHWAYS.find(p => p.id === state.selectedPath);
      if (!pathData) return;

      // Rebuild tab strip (path may have changed)
      document.getElementById('tab-strip').innerHTML = buildTabsHTML(pathData);

      // ... rest of render() as above ...
    }
    ```

    **8. Initial render on page load:**
    At the end of the script block, call render() to display the default path+level:
    ```javascript
    // Initial render: Dynamic Leadership, Level 1
    render();
    ```

    **9. Also update the path `<select>` to include all paths from PATHWAYS data:**
    After the render() call, dynamically populate the path selector from PATHWAYS data:
    ```javascript
    // Populate path selector from data
    (function populatePathSelector() {
      const selector = document.getElementById('path-selector');
      selector.innerHTML = PATHWAYS.map(p =>
        `<option value="${p.id}"${p.id === state.selectedPath ? ' selected' : ''}>${p.name}</option>`
      ).join('');
    })();
    ```

    Do NOT add any event listeners in this plan — that is Plan 03's responsibility. The selector will be populated but not yet wired.
  </action>
  <verify>
    <automated>powershell.exe -Command "Select-String -Path 'C:\Users\redtr\.claude\toastmasters-pathways-app\pathways-app.html' -Pattern 'buildProjectCardHTML|buildEnhancementHTML|buildLevelHTML|function render' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <acceptance_criteria>
    - pathways-app.html contains `function buildProjectCardHTML`
    - pathways-app.html contains `function buildEnhancementHTML`
    - pathways-app.html contains `function buildLevelHTML`
    - pathways-app.html contains `function render(`
    - pathways-app.html contains `function buildTabsHTML`
    - pathways-app.html contains `JSON.parse(document.getElementById('pathways-data').text)`
    - pathways-app.html contains `PATHWAYS.find(p => p.id === state.selectedPath)`
    - pathways-app.html contains `contentPane.innerHTML = buildLevelHTML`
    - pathways-app.html contains `if (!enhancement) return '';`
    - pathways-app.html contains `project.type === 'Required' ? 'badge-required' : 'badge-elective'`
    - pathways-app.html contains `.map((project, i) => buildProjectCardHTML(project, pathId, levelIndex, i))`
    - pathways-app.html contains `render();` at the bottom of the script block (initial call)
    - pathways-app.html does NOT contain `innerHTML` reads (render only writes to contentPane, never reads from it)
    - Opening pathways-app.html in a browser shows Dynamic Leadership Level 1 content: Enhancement section with roles, then project cards with badges and time values
    - Communication Series does NOT show an Enhancement section for any of its 5 levels
    - All project cards show name, badge (Required/Elective), time (with en-dash), and purpose text
  </acceptance_criteria>
  <done>Opening pathways-app.html shows Dynamic Leadership Level 1 with an Enhancement section (Table Topic Speaker, Evaluator, Timer OR Ah-Counter) above the project cards. Switching to Communication Series (manually changing state in console) shows project cards with no Enhancement section. All cards display name, badge, time, purpose inline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON data block → JavaScript | Data parsed from embedded script block |
| Template literal output → innerHTML | HTML string written to content pane |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Embedded JSON data | accept | Data is static curriculum content embedded at build time. No user input flows into the JSON block. Read-only at runtime. |
| T-02-02 | Tampering | innerHTML assignment | mitigate | All content comes from the embedded PATHWAYS constant (developer-controlled). No user-supplied strings ever reach innerHTML. buildProjectCardHTML and buildEnhancementHTML only interpolate values from the static data object. If project names or purpose text ever come from user input in a future phase, switch to textContent assignment or DOMPurify. |
| T-02-03 | Spoofing | Type field in JSON ("Required"/"Elective") | accept | Strict enum enforced by developer at data-entry time. Badge rendering is cosmetic; no security consequence if a wrong badge is shown. Phase 2 adds a validation script (DATA-02). |
</threat_model>

<verification>
After both tasks complete, open pathways-app.html in a browser and verify:

1. Page loads showing Dynamic Leadership Level 1 content automatically (no user gesture)
2. Enhancement section is visible above the project cards with role pills (Table Topic Speaker, Evaluator, Timer OR Ah-Counter)
3. Ice Breaker card shows: "Ice Breaker" heading, "Required" blue badge, "4–6 min" (en-dash), purpose text
4. A "Elective" card shows yellow badge with black text
5. Open browser console, run: `state` — should show `{ selectedPath: 'dynamic-leadership', selectedLevel: 0 }`
6. Run in console: `setState({ selectedPath: 'communication-series', selectedLevel: 0 })` — page should update to show Communication Series Level 1 with NO Enhancement section
7. Run: `setState({ selectedPath: 'dynamic-leadership', selectedLevel: 2 })` — should show Dynamic Leadership Level 3 content
</verification>

<success_criteria>
- Embedded JSON includes dataVersion and lastVerified fields (DATA-04)
- enhancement stored as separate level field, not inside projects[] (DATA-05)
- Level name and completionRequirement render above project list (CONT-01)
- Enhancement section renders for Dynamic Leadership (current path) (CONT-02)
- Each project card shows name, badge, time, purpose inline (CONT-03, CONT-04, CONT-05, CONT-06)
- Communication Series levels render with no Enhancement section (CONT-07)
- Path selector populated from PATHWAYS data (NAV-01)
- 5 level tabs rendered per path from level data (NAV-03)
- Only selected path+level DOM is rendered at any time (PERF-01 enforced by render() architecture)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` following the summary template.
</output>
